library(devtools) 
library(testthat)
library(FELLA)
message(
  "Welcome home! If you want to load something ", 
  "each time R is restarted edit .Rprofile")

# symbolic link to database
# local({
#   file.db <- "~/R/x86_64-pc-linux-gnu-library/database/"
#   local.db <- "~/R/x86_64-pc-linux-gnu-library/3.2/FELLA/"
#   
#   if (file.exists(paste0(local.db, "database"))) {
#     message("Database is already present")
#   } else {
#     message("Database not present. Symlinking...")
#     file.symlink(file.db, local.db)
#   }
#   
#   invisible()
# })
